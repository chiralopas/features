HOW TO BUILD

- should have a working c++ cmaketools environment with ninja for vscode
- should have vcpkg installed and its path available via variable VCPKG_ROOT
- select native toolkit for given platform then configure build and run
